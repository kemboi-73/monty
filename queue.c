#include "monty.h"
/**
 * c_queue - prints the top
 * @head: stack head
 * @count: line_number
 * Return: no return
*/
void c_queue(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 1;
}
